# HIAS-CSAdeepseek 项目维护约定

## 修改记录

每次修改本项目的规则、课程数据、培养方案、推荐逻辑、界面、兼容性或测试，都必须同步更新：

`docs/修改记录.md`

新增记录至少包含修改日期、需求背景、涉及文件、实现逻辑、用户可见变化、兼容性、测试结果、本地预览状态和待确认事项。不要只修改代码而跳过主文档记录。

## 受控修改

- 保留既有功能、localStorage、备份恢复、离线版和 WakeUp CSV 格式。
- 不删除或重新生成既有课程数据，除非用户明确要求并完成审计。
- 新增学院或培养方向时，优先补充学院目录和培养方案数据，让筛选分组自动生成，不在页面组件中硬编码学院列表。
- 规则使用确定性代码执行；不把学校培养硬规则交给 AI 自行判断。
- 发布前先完成本地预览和测试，未得到明确授权不得自动推送或替换线上版本。

## 规则口径（重要）

- HIAS讲堂（人文系列讲座）计入**公共选修体系**，不计入专业非学位。
- 科学前沿讲座计入**专业非学位课（专业选修）**，不计入公共选修。
- 两者都不计入秋季/春季有效学分的最低 10 学分门槛。
- 口径来源：与同源参考项目 `cty-cty/HIAS-CSA` 核对学校《研究生课程学习与选课须知(2026-2027)》后的结论。

## 本地验证

```bash
npm run test:rules   # 纯 TypeScript 规则测试
npx tsc --noEmit     # 类型检查
```

`test:rules` 通过 Node 原生类型剥离直接执行 `tests/program-rules.test.ts`，
由 `scripts/ts-extension-loader.mjs` 解析无扩展名的相对导入。
**不要再改回基于 esbuild 打包的 runner**：esbuild 需要 spawn 子进程并通过管道取回输出，
在受限/沙箱环境会直接 `EPERM` 失败。

## 部署注意

`.github/workflows/pages.yml` 必须把 `public/` 一起拷进 `_site`，否则线上的
`sw.js`、`manifest.webmanifest`、`favicon.svg` 与 PWA 图标会全部 404
（离线单文件 HTML 的 `<head>` 引用了这些相对路径资源）。
