# HIAS-CSA 工作日志

记录每次改动的**目的、涉及文件、验证结果与遗留风险**。

**约定**

- 每次改动追加一节，不修改历史条目（除非是更正，更正需显式标注）。
- 凡是没有实际跑过、或需要人眼确认的结论，一律标注为「未验证」，不写成已完成。
- 审计提出的怀疑若被复核推翻，也记录在此，避免下次重复踩。
- **（2026-09-11 起）改动不再逐次等待审核**：做完即提交、推送上线、核验线上，然后汇报。
- **（2026-09-11 起）工作日志与代码改动合并在同一个提交**，避免为一个日志多触发一次 Pages 部署。
- **推送结果以 `git ls-remote` 为准**：不要相信被管道截断的 `git push` 输出（见「部署上线记录」中的坑）。

**项目信息**

| 项 | 值 |
| --- | --- |
| 项目 | HIAS-CSA 2026 秋季预选课助手（国科大杭高院） |
| 工作副本 | `D:\桌面\DSH\work\HIAS-CSA` |
| 原仓库 | `D:\桌面\日常\HIAS-CSA` |
| 线上 | <https://cty-cty.github.io/HIAS-CSAdeepseek/> |
| 本次基线提交 | `c551bbf` |
| 当前线上提交 | `f79b070`（已上线，GitHub Actions 运行结论 success） |

---

## 2026-09-11 · 优化批次一（数据安全 / 部署 / 打印 / 移动端 / 无障碍）

**状态**：已完成、已构建、**已于 2026-09-11 推送并上线**（详见文末「部署上线记录」）
**改动规模**：15 个文件，+775 / −127（不含工作日志本身）
**基线提交**：`c551bbf`

### 1. 数据安全

**问题 1 · 导入的课程数据会静默消失**

`app/course-explorer.tsx` 读取 localStorage 时，`try` 只包住了 `JSON.parse`，紧随其后的结构校验
（`dataset.courses.every(isCourse)`）在 `try` 之外。校验不通过时 `parsedDatasets` 保持空数组，
既不报错也不删除 key，用户每次打开都会丢一次，且没有任何提示。

**改法**：改为逐条容错。合法数据集保留，非法课程按条剔除并计数；只要发生剔除，就把原始字符串
另存到 `hias-course-datasets-backup-v1`（常量在第 227 行），并通过 `setDataError` 明确告知
（第 1308 行），不再整份丢弃。

**问题 2 · localStorage 写入无保护，配额超限直接白屏**

五处持久化 effect 直接调用 `setItem`，`QuotaExceededError` 抛在 effect 内会导致 React 19 卸载
根节点；而项目里**没有任何 error boundary**（`error.tsx` / `global-error.tsx` / `ErrorBoundary` 全无）。

**改法**

- 新增 `safeSetItem`（第 234 行）与 `safeSetJSON`（第 244 行）两个写入保护函数；
- 五处持久化 effect 全部改用它们，失败时由 `reportPersistFailure`（第 1067 行）显示提示条，建议导出备份；
- 新增 `components/error-boundary.tsx`（165 行）作为顶层兜底，提供「尝试恢复页面 / 下载本地数据备份 /
  重新载入 / 清除本地数据并重新载入」四条自救路径；
- 在 `offline/main.tsx` 与 `app/layout.tsx` 两处入口接入。

### 2. 部署元数据与图标

**问题**：`.github/workflows/pages.yml` 只把单文件 HTML 发布为 `index.html`，仓库 `public/` 里现成的
`favicon.svg`、`og.png`、`manifest.webmanifest`、`sw.js` 一个都没上线 → `/favicon.ico` 404、
分享链接没有预览卡片、无法安装为应用。

**另一处隐患**：站点部署在子路径 `/HIAS-CSAdeepseek/` 下，而 manifest 与 `sw.js` 使用绝对路径
（`/favicon.svg`、`start_url: "/"`、`CORE_ASSETS: ['/']`），会指向域名根目录而 404。

**改法**

| 文件 | 改动 |
| --- | --- |
| `scripts/prepare-pages.mjs`（新增，143 行） | 生成 `_site/`：注入 description / manifest / OG / Twitter 元数据与 Service Worker 注册的 `index.html`、`404.html`、`.nojekyll`，并复制 `favicon.svg`、`og.png`、`manifest.webmanifest`、`sw.js` |
| `.github/workflows/pages.yml` | 原来的 `cp + sed` 两步替换为 `node scripts/prepare-pages.mjs` |
| `public/manifest.webmanifest` | `start_url` / `scope` / `icons` 改为相对路径 |
| `public/sw.js` | 全部地址改为基于 `self.registration.scope` 计算；导航请求网络优先、离线回退缓存；站外请求不接管；缓存名 v15 → v16 |
| `scripts/package-offline.mjs` | 离线单文件补 `description`，并用 **data URI 内联 favicon**，保证转发给别人时图标不丢 |
| `scripts/validate-offline.mjs` | 原校验禁止任何 `<link href>`，会误杀内联图标；改为只禁止非 `data:` 的外部资源，并在报错中列出具体地址 |
| `.gitignore` | 忽略构建产物 `/_site/` |

### 3. 打印 / 导出 PDF

**问题**：整个打印样式只有 16 行，仅隐藏 5 个选择器。深色提示条与交互控件照打，课程卡片会被分页
从中间切断，960px 的网格课表在 A4 上放不下。而"打一张课表"是选课工具的高频用法。

**改法**（`app/globals.css`）

- `@page { margin: 12mm }`；
- 打印时隐藏导航、筛选栏、方案侧栏、移动底栏、提示条、回到顶部、弹窗遮罩与关闭按钮；
- 弹窗（课表）在打印时改为静态定位，不再作为浮层；
- `break-inside: avoid` 避免卡片与课程行被分页切断；
- **用竖版清单替代网格课表**：`.timetable-grid-wrap` 隐藏、`.timetable-cards` 显示，并保留课表配色
  （`print-color-adjust: exact`）。

### 4. 移动端课表（功能回退修复）

**问题**：`e8421ef` 曾实现"移动端竖版卡片课表"，但在 `37dfd06`（整体替换为采纳内容）时丢失 ——
当前代码里 `md:` / `lg:` 变体数量为 0，`weekSchedulesByDay` / `WeekScheduleCard` 都不存在。
手机上只能在 90vh 弹窗里横向拖动 960px 网格，而弹窗说明还写着"桌面端横向滚动可查看完整一周"，
暗示手机另有视图。

**改法**：从 git 历史取回实现并适配当前代码。

- 新增 `WeekScheduleCard` 类型与 `weekSchedulesByDay`（第 2106 行）：只取"第 N 周有课"的安排，
  按星期分组、按时段排序；
- 桌面网格容器加 `.timetable-grid-wrap`（第 5380 行）并限定 `md:block`；
- 新增 `.timetable-cards md:hidden`（第 5456 行）竖版清单：节次色块 + 课程名 + 教室 + 周次，
  有冲突标红，点击进详情；
- 修正弹窗内的说明文案。

### 5. 无障碍与 CSS

| 项 | 改动 |
| --- | --- |
| 焦点环 | 选择器补 `textarea` / `summary` / `[tabindex]` / `[role='button']`；跳转链接的目标 `<TabsContent tabIndex={0}>` 原本拿不到任何焦点提示 |
| 弹窗焦点 | `components/ui/dialog.tsx` 的 `outline-none` 补 `focus-visible:ring-2` |
| 读屏播报 | 操作提示条与"已显示 N / M"结果数加 `aria-live`（基线核实为 0 处，现为 2 处） |
| 减少动态效果 | 提示条出场动画靠 2.74s 延迟触发，全局压缩时长会把它提前打到 `opacity:0`，反而让开启该选项的用户提前失去"撤销"按钮 → 改为 `.selection-toast { animation: none }`，交给 JS 的 3s 定时移除 |
| 触控目标 | 筛选 chip `2.4rem → 2.75rem`，提示条按钮 `2.2rem → 2.75rem`，统一到 44px |
| 字号 | 37 处低于 12px 的 `font-size`（0.66–0.74rem）统一抬到 `0.75rem`；4 处 `text-[11px]` 抬到 `text-[12px]` |
| 配色方案 | 补 `:root { color-scheme: light }`，避免系统深色模式下原生控件（复选框 / 下拉 / 滚动条）变深色与本页冲突 |

### 6. 顺带修复的构建故障

**问题**：`npm run build:offline` 在本机（Windows + Node 24）**原本就跑不起来**。Vite 8 默认用
rolldown 打包配置文件，加载 `@tailwindcss/oxide-win32-x64-msvc` 原生模块时报
`UNLOADABLE_DEPENDENCY`。已在原仓库复现同样错误，与本次改动无关。

**改法**：`package.json` 的 `build:offline` 加 `--configLoader native`（交给 Node 直接加载配置）；
`engines.node` 提到 `>=22.18`；新增 `build:pages` 一条命令跑完整链路。

### 7. 文档

`README.md`：补充移动端卡片课表、打印 / 导出 PDF、数据丢失防护三项新行为；新增「生成线上发布目录」
一节；写入构建参数说明与回滚方法。

---

## 验证记录（2026-09-11）

| 项目 | 方式 | 结果 |
| --- | --- | --- |
| 类型检查 | `tsc -p tsconfig.json --noEmit` | 0 错误 |
| Lint | `oxlint` | 0 错误 0 警告（85 文件 / 203 规则） |
| 离线构建 | `vite build --configLoader native` | 通过；app.css 286.63 kB、app.js 603.11 kB |
| 离线校验 | `scripts/validate-offline.mjs` | 通过：158 门课程、0.85 MB、无外部脚本与样式 |
| Pages 生成 | `scripts/prepare-pages.mjs` | 通过：index.html + favicon + og.png + manifest + sw.js + 404.html |
| 渲染冒烟 | SSR 渲染主组件（Node） | 通过：172,939 字节 HTML、24 张课程卡片、无异常 |
| Service Worker | Node mock 执行 `public/sw.js` | 通过：首页与资源缓存全部落在 scope 内、无裸根路径、离线回退生效、站外请求不接管 |
| 内联图标 | data URI 解码比对 | 通过：1051 字符，解码后与 `favicon.svg` 逐字节一致 |
| OG 图 | 读 PNG 头 | 1200×630，862 KB（符合分享卡片尺寸） |
| 文件完整性 | CRLF/LF 与末尾换行检查 | 纯 LF，末尾换行保留，批量替换未误伤 |
| **浏览器实测** | — | **未做**：沙箱阻止 Chrome 多进程 IPC（命名管道），无法截图或做 DOM 测量 |
| `npm run test:rules` | — | **未做**：esbuild 需要命名管道，`spawn EPERM`；该套件覆盖的 `program-rules.ts` 本次未改动 |
| 打印效果 | — | **未做**：需人工 Ctrl+P 确认 |
| 移动端课表视觉 | — | **未做**：需人工在窄屏确认 |

### 未验证 / 风险

- **字号统一是本次唯一有布局风险的改动**：37 处字号从 10.6–11.8px 抬到 12px（约 +13%），
  理论上可能造成换行或拥挤，需人工扫一眼。已确认无低于 12px 的残留。
- **CI 风险**：`--configLoader native` 会影响 GitHub Actions（Linux 上默认加载器本来可用）。
  若 CI 报错，去掉该参数即可；已写入 README。
- Service Worker 首次上线后，老访客需要一次导航才会拿到新版本；导航是网络优先，不会卡在旧缓存。

---

## 复核后推翻的结论（避免重复踩）

- **断点顺序不是 bug**：`@media (max-width: 580px)` 排在 `700px` 之前看似事故，但 700px 块是后来
  专门修"手机头部学期切换重叠"的（`0ed5df5`），故意覆盖 580px 的旧规则。调整顺序会退回重叠 bug，故未改。
- **移动端课表没有被裁切**："约 600px 列不可达"不成立 —— 外层有 `overflow-x-auto`，是可以滚动的，
  真正的问题是"能滚但难用"，已按此口径修复。
- **提权命令的写入失效判断有误**：曾判断沙箱提权运行的写入不落盘，后被探针文件推翻，写入是可靠的。

---

## 已明确不做（避免误以为遗漏）

- **颜色 token 化**（454 个 hex 字面量 / 323 个不同颜色，而 `:root` 只有 19 个 token —— 两个数字均已复核）
  与**暗色模式**：属重构而非优化，混入会放大评审面，建议单独排期（先 token 化，再上暗色）。
- **死 CSS 清理**（审计统计约 65 个类在 TSX 中已无引用；我抽查 `.view-tab`、`.hero-panel`、
  `.star-button`、`.credit-spotlight`、`.english-status` 引用数均为 0）：不影响用户，
  需逐类核对，收益低风险中等。
- **单文件 859KB 改为多资源构建**：可显著改善首屏与缓存，但与"离线可分发"的产品定位冲突，
  需要产品决策；本次先接入 Service Worker 改善重复访问与弱网体验。

---

## 2026-09-11 · 部署上线记录

**状态**：已上线并验证通过
**提交**：`c036246`（父提交 `c551bbf`）
**推送**：`c551bbf..c036246  main -> main`

### 过程与遇到的问题

1. **首次推送失败**：`Failed to connect to github.com port 443 via 127.0.0.1`。
   原因是环境变量指定了代理 `HTTPS_PROXY=http://127.0.0.1:7899`（FlClash），
   当时该代理瞬时不可达；同一时刻直连也报 `Recv failure: Connection was reset`。
2. **诊断**：`netstat` 显示 `127.0.0.1:7899` 由 `FlClashCore` 监听，端口正常；
   数分钟后复测，TCP 连接、curl 直连、curl 走代理三种方式均恢复正常（HTTP 200）。
   → 判定为代理客户端的瞬时抖动，不是配置错误。
3. **推送成功**：使用交接文档记录的配方 `git -c http.sslBackend=openssl push origin main`
   （本机 system 级 `http.sslBackend = schannel`，直连时 TLS 会被重置）。

> 经验：本项目推送若报 TLS 相关错误，先确认 FlClash 是否在运行，再加 `-c http.sslBackend=openssl`。

### 一个会误导人的坑：不要把 `git push` 的输出用管道截断

本次推送时用了 `git push ... 2>&1 | Select-Object -First 5`，结果报出：

```
! [remote rejected] main -> main (cannot lock ref 'refs/heads/main':
  is at 809bdfc... but expected f79b070...)
error: failed to push some refs
```

**但远端实际上已经更新成功** —— `git ls-remote` 显示远端就是 `809bdfc`。
原因是 PowerShell 在取够 N 行后关闭管道，git 的 stdout 写入失败并中断，于是报出这条与实际结果不符的错误。
改用不截断的 `git push origin main:main` 重跑，得到 `exit=0 / Everything up-to-date`，无任何报错。

> 结论：**推送后一律用 `git ls-remote` 判定结果，不要相信被截断的 push 输出。**
> 后续所有 `git push` / `git fetch` 都不再套 `Select-Object`。

（这一条在 2026-09-11 的两次推送中都复现过：`c036246→2370327` 与 `f79b070→809bdfc`。）

### 上线验证

**GitHub Actions**（`api.github.com/repos/cty-cty/HIAS-CSAdeepseek/actions/runs`）

| sha | status | conclusion |
| --- | --- | --- |
| `c036246` | completed | **success** |

→ 说明 `--configLoader native` **没有**影响 Linux CI（此前标记的 CI 风险不成立）。

**线上资源可达性**

| 路径 | 上线前 | 上线后 |
| --- | --- | --- |
| `/` | 200（约 880 KB，旧版） | 200（893,106 字节，新版） |
| `/index.html` | 200 | 200 |
| `/favicon.svg` | **404** | **200**（image/svg+xml, 712 B） |
| `/manifest.webmanifest` | **404** | **200**（application/manifest+json, 554 B） |
| `/sw.js` | **404** | **200**（application/javascript, 3,076 B） |
| `/og.png` | 404（未发布） | 200（882,199 B） |
| `/404.html` | 404（GitHub 默认页） | 200（自定义页, 1,278 B） |

**线上页面内容**

| 检查项 | 结果 |
| --- | --- |
| `<title>` | `HIAS-CSA · 2026 秋季预选课助手`（「（离线版）」已去除） |
| `og:image` 分享元数据 | 有 |
| `name="description"` | 有 |
| `rel="manifest"` | 有 |
| Service Worker 注册 | 有 |
| 移动端卡片课表（`timetable-cards`） | 有 |
| 错误边界（`下载本地数据备份`） | 有 |
| 数据备份键（`hias-course-datasets-backup-v1`） | 有 |

---

## 待办

- [x] 用户审核本地预览 <http://127.0.0.1:8817/>
- [x] 提交 → 推送 `main` → 确认 GitHub Actions 构建与线上页面
- [x] 推送后把原仓库 `D:\桌面\日常\HIAS-CSA` 同步到同一提交
- [ ] 更新已过期的 `D:\桌面\日常\HIAS-CSA-HANDOFF.md`（其中 HEAD、路径、以及"移动端竖版卡片视图已做"等描述均已不成立）
- [ ] 浏览器实测打印效果与移动端课表视觉（沙箱内无法完成，需人眼确认）

---

## 2026-09-11 · 数据修正：「光电子材料与器件」教室改为 13-312

**状态**：已上线
**改动规模**：1 个文件，+1 / −1
**提交**：`f79b070`（父提交 `2370327`）
**推送**：`2370327..f79b070  main -> main`

### 改动内容

课程 `280216085408P2005`「光电子材料与器件」（id 72，黄志明，专业核心课，3 学分 / 60 学时）
的两条排课记录，教室由 `13-110` 改为 `13-312`：

| 上课时间 | 周次 |
| --- | --- |
| 周二 第 1-3 节 | 第 3-5,7-13 周 |
| 周四 第 1-3 节 | 第 2-4,6-12 周 |

来源：用户提供的最新教室信息。

> 注意区分：id 46「**石墨烯及类石墨烯**光电子材料与器件」（毛旭瑞，研讨课，13-117）是另一门课，未改动。

### 改法与可复用经验

`app/courses.json` 是**单行压缩 JSON**（86,167 字符、1 行），无法手工编辑，也不能靠文本 diff 判断改动范围
（任何一处改动都会让整行显示为变更）。本次流程：

1. **先做无损往返校验**：确认 `JSON.stringify(JSON.parse(raw)) === raw` 且末尾换行状态一致，
   只有通过才写入 —— 避免脚本改写引入格式噪声；
2. 按课程 `id` 定位，修改 `schedules[].room`；
3. 重新序列化写回（键顺序由 `JSON.parse` 保持，字符数不变）；
4. **用「解析后语义比较」核对范围**：`git stash` 前后各解析一次，逐课程比对 `JSON.stringify(course.schedules)`，
   确认**只有 1 门课程**变化。

一次性脚本：`D:\桌面\DSH\audit\set-room.mjs`（改写）、`verify-room.mjs`（核对）。

### 影响范围

改后全库使用 `13-312` 的排课共 4 条：id 39（生物学与分子生物学）、id 108（创新型个性发展心理学）
本就占用该教室、未受影响，加上 id 72 的 2 条。

### 验证记录

| 项目 | 方式 | 结果 |
| --- | --- | --- |
| 无损往返 | 脚本内断言 | 通过（86167 → 86167 字符，逐字节一致） |
| 改动范围 | 解析后语义比较 | 仅 id 72 变化 |
| 离线构建 | `vite build --configLoader native` | 通过 |
| 离线校验 | `scripts/validate-offline.mjs` | 通过（158 门课程、0.85 MB） |
| Pages 生成 | `scripts/prepare-pages.mjs` | 通过 |
| GitHub Actions | `f79b070` 运行结论 | **success** |
| 线上页面 | 抓取线上 HTML 定位课程编码 | 该课程记录两条排课均为 `room":"13-312"`，片段内无 `13-110` |

### 未验证

- 手机 / 桌面上的实际显示效果（沙箱内无法做浏览器实测）。

---

## 后续条目模板

````markdown
## YYYY-MM-DD · <主题>

**状态**：<进行中 / 已完成待审核 / 已上线>
**改动规模**：N 个文件，+A / −B
**基线提交**：<sha>

### 1. <子项>

- 问题：
- 改法：
- 涉及文件：
- 验证：

### 验证记录

| 项目 | 方式 | 结果 |
| --- | --- | --- |

### 未验证 / 风险

-
````
